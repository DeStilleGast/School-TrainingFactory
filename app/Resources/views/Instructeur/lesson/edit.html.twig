{% extends 'baseLoggedIn.twig' %}

{% block body %}
    <h1>Lesson edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}


    <table class="col-12">
        <tr>
            <td>
                <a class="btn btn-primary" href="{{ path('Instructeurs_index') }}">Back to the list</a>
                <button class="btn btn-success" type="submit" value="Edit"><i class="fas fa-edit"></i> Edit</button>
                {{ form_end(edit_form) }}

            </td>
            <td class="float-right">
                {{ form_start(delete_form) }}
                <button class="btn btn-danger" type="submit" value="Delete"><i class="fas fa-trash"></i> Delete</button>
                {{ form_end(delete_form) }}
            </td>
        </tr>
    </table>
{% endblock %}
