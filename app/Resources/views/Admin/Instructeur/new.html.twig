{% extends 'baseLoggedIn.twig' %}

{% block body %}
    <h1>Instructeur creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

    <table>
        <tr>
            <td>
                <input class="btn btn-success" type="submit" value="Create" />
            </td>
            {{ form_end(form) }}
            <td>
                <a class="btn btn-primary" href="{{ path('admin_instructeur_index') }}">Back to the list</a>
            </td>
        </tr>
    </table>
{% endblock %}
